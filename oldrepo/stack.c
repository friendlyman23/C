#include <stdio.h>

#define STACK_SIZE 10

void is_full(void);
void is_empty(void);
void pop(void);
void push(int item);

int stack[10];
int *top_ptr = &stack[0];

int main(void)
{
	//push to the stack
    push(1);
    push(2);
    push(3);
    
  //pop from the stack
    











	return 0;

}
